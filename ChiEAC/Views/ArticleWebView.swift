// Deprecated: replaced by ExternalLinkWebView. Left in repo but excluded from build with #if false.
#if false
import SwiftUI
import WebKit

struct ArticleWebView: View {
    let urlString: String
    let title: String
    var body: some View { Text("Deprecated ArticleWebView") }
}
#endif
